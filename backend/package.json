{
  "name": "e-samanvit-backend",
  "version": "1.0.0",
  "private": true,
  "description": "e-Samanvit backend (SIH26129) — consent-driven interoperability layer prototype: mock government adapters, consent manager, service registry/schemas, application submission, and application tracking. See the root README for full scope and known limitations.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node scripts/check-syntax.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {}
}
