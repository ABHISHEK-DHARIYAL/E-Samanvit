/**
 * Status routes — PLACEHOLDER.
 *
 * Will eventually expose the Status Aggregator's unified view of a
 * citizen's applications across departments (the existing frontend
 * mock-api.js timeline model is the likely reference for this shape).
 * Not implemented yet.
 */
const express = require('express');
const router = express.Router();

module.exports = router;
