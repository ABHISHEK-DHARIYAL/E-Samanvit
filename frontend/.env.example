# e-Samanvit frontend — non-secret configuration only.
# This project has no bundler, so this value is read by build.js and
# written into js/config.js at build time (see build.js for details).
# Copy this file to ".env" for local development and adjust as needed.

API_BASE_URL=http://localhost:5000
